// Audrey implements this screen.
// Shows impl_where / impl_what_first fields conditionally based on profile.current_device >= 1
// Shows corrected_duration banner when backend returns corrected_duration != planned_duration
export default function AddTask() {
  return <div>Add Task — TODO (Audrey)</div>;
}
